{
  "name": "patch-notes-blog",
  "version": "2.0.0",
  "private": true,
  "description": "Hub d'actualité patch-notes.fr - serveur SSR + API.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test automation/**/*.test.js *.test.js",
    "test:pin-race": "node --test integration/pin-race.test.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "node-cron": "^4.6.0",
    "prisma": "^5.22.0"
  }
}
